# WebSSH v3.4.0 — Cloudflare 补全 · 部署数据保护 · 全端自动构建

在 3.0.0 六端统一基线之上，本版本集中修复 Cloudflare 部署的功能缺口与数据安全边界，打通 GitHub 全端自动构建，并全面通俗化密码相关文档。

---

## ✨ 本版本亮点

- ☁️ **Cloudflare 全面补全**：修复监控轮询打进终端的问题、强制校验访问密码（此前未实现）、新增 Model API——MCP 现在可以直接指向 CF 部署
- 🛡️ **部署不再丢数据**：一键部署脚本更新时保留服务器注册表 / 审计日志 / 访问密码等运行时数据；Docker 数据卷路径修正
- 🤖 **GitHub 全端自动构建**：推送版本 tag 即自动产出 Windows / macOS / Android / iOS 安装包并挂到 Release
- 📖 **文档通俗化**：「后端令牌」改称「后端访问密码」——就是你自己定的一个密码；不设时服务器自动生成临时密码，打开即用
- 📍 **内网 IP 入口更醒目**：设置面板改名「后端网关地址（填内网 IP）」，手机连内网服务器不再找不到地方

（以下为 3.0.0 基线的完整能力概览）

- 🖥️ **六端统一**：Web / Windows 便携版 / macOS dmg / Android APK / iOS / Cloudflare Workers
- 🔐 **安全全面加固**：鉴权令牌去 URL 化、本地凭据 AES-GCM 加密、主密码一体化备份
- 📊 **FinalShell 式主机监控**：终端内实时查看 CPU / 内存 / 负载 / 磁盘 / 网速，免装 agent
- 🪟 **真分屏终端**：垂直 / 水平分屏，多会话同屏，分隔条可拖动
- 📱 **移动端重做**：卡片式服务器列表、抽屉式弹窗、触屏友好
- 🤖 **MCP 支持**：Claude / Cursor 可直接管理你的服务器（Web / 桌面 / CF 均可）

---

## 🖥️ 平台覆盖

| 平台 | 形态 | 获取方式 |
|---|---|---|
| Web | 浏览器 | `npm run dev:all` 或 Docker |
| Windows | 免安装便携版 | `npm run desktop`（或打 tag 自动构建） |
| macOS | dmg + zip（双架构） | `npm run desktop:mac`（或打 tag 自动构建） |
| Android | APK | `npm run cap:build:android`（或打 tag 自动构建） |
| iOS | Xcode 工程 / 未签名包 | `npm run build && npx cap sync ios`（或打 tag 自动构建） |
| Cloudflare | Workers / Pages | `npm run worker:deploy` / Pages 连接 GitHub 自动部署 |

> 🤖 **自动构建**：`git tag v3.4.0 && git push origin v3.4.0`，GitHub Actions 自动产出上述全部安装包并附加到 Release。

- **macOS 新增支持**：托盘、端口管理、图标全套适配
- **Android 深度适配**：边到边全屏、明文流量配置、键盘/状态栏插件、屏幕常亮、终端功能键栏
- **iOS 工程就绪**：ATS 例外、清除混合内容隐患、arm64 清单修正

## 🔐 安全

- **令牌去 URL 化**：WebSocket 鉴权令牌改走 `Sec-WebSocket-Protocol` 子协议传输，不再出现在 URL / 日志中（旧 query 方式保留为回退兼容）
- **本地凭据 AES 化**：本机记住的凭据升级为 AES-GCM（主密码 PBKDF2 派生），旧数据自动迁移
- **备份默认主密码加密**：跨端恢复只记一个密码；修改密码时已记住的凭据自动轮换重加密
- **忘记密码全清**：登录页一键清空全部本地数据，防止信息泄露
- **设备免密登录**：内网个人设备可「记住本设备」，下次打开免输密码
- **TOFU 主机密钥校验**：首次连接记录指纹，密钥变更即拒绝连接，防中间人

## 🚀 新功能

- **主机监控面板**：终端底部实时显示 CPU / 内存 / 负载 / 磁盘 / 网速 / 运行时长，含最近 2 分钟历史曲线，免 agent
- **终端真分屏**：垂直 / 水平两种布局，多会话同屏操作，分隔条拖动调比例，点选聚焦
- **SSH config 导入**：一键导入 `~/.ssh/config` 批量添加主机
- **MCP Agent 接入**：Claude / Cursor 等 MCP 客户端可列出服务器、探测、执行命令、增删服务器
- **内网地址发现**：设置页自动列出服务端局域网地址，点击复制，手机粘贴即连
- **备份多端通用**：加密备份文件任意端导出 / 导入 / 云同步
- **新手引导**：首次进入三步上手指南

## 🎨 体验

- **移动端 Termius 化**：卡片式服务器列表、全部弹窗底部抽屉化、输入防缩放、44px 触控目标
- **面板设计系统**：六个弹出面板统一视觉与交互
- **文件管理触屏优化**：操作按钮触屏常显、单击进目录
- **双语完整**：500+ 键位中英全覆盖

## ✅ 质量

- 服务端测试 **51 项全绿**；新增端到端冒烟测试 `npm run smoke`（真实起服务 + 模拟 SSH 目标全链路验证）
- 类型检查全量通过；Cloudflare Worker 保持独立实现，不受本次升级影响

## ⚠️ 升级须知

- **公网部署**：必须设置一个后端访问密码（环境变量 `AUTH_TOKEN`，自己随便定一个即可）并启用 HTTPS，详见 `SECURITY.md`
- **Cloudflare 用户注意**：CF 端现已强制校验后端访问密码（此前未实现校验），未配置时接口一律拒绝服务——重新部署前请先在环境变量里补上 `AUTH_TOKEN=你自己定的密码`。CF 端同时新增 Model API（可选绑定 `MODEL_REGISTRY` KV），MCP 桥可直接指向 CF 地址
- **不想记密码？**：自建服务器版不设 `AUTH_TOKEN` 时，每次启动会自动生成临时访问密码并直接传给页面，浏览器打开即用（重启会更换；公网部署 / 手机远程连 / MCP 仍需固定设置）
- **部署脚本**：`scripts/deploy.sh` 更新时不再清除运行时数据（服务器注册表、审计日志、`.env` 等），重新部署不会丢失已保存的信息
- **旧凭据自动迁移**：首次打开新版本时，旧的本地凭据会自动升级为加密格式，无需手动操作

## 📦 快速开始

```bash
git clone https://github.com/<你的用户名>/webssh.git
cd webssh
npm install
npm run dev:all     # 启动后端 + 前端，浏览器打开即可
```

---

完整变更细节见 [CHANGELOG.md](./CHANGELOG.md)；安全模型见 [SECURITY.md](./SECURITY.md)。
